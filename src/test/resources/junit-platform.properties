# Disable JUnit 5 parallel execution - tests run sequentially within each class
# Gradle's maxParallelForks will run multiple test classes in separate JVM processes
# This avoids XML test result file writing conflicts while still allowing class-level parallelism
junit.jupiter.execution.parallel.enabled=false
